#include <bits/stdc++.h>

using namespace std;

typedef long long int LL;

int main() {
    int n, m;
    scanf("%d%d", &n, &m);
    pair<LL, LL>* arr = (pair<LL, LL>*)malloc(n * sizeof(pair<LL, LL>));
    for (int i = 0; i < m; i++)
    {
        
    }
}