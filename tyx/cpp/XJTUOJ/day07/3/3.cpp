#include <bits/stdc++.h>

using namespace std;

typedef struct _node
{
    int mapin;
    int mapout;
    vector<int> _out;
} Node;

int main() {
    int n, m;
    scanf("%d%d", &n, &m);
    Node* node = (Node*)malloc((n + 5) * sizeof(Node));
}